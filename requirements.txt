# Core
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
pyyaml>=6.0
hmmlearn>=0.3
matplotlib>=3.7

# Live data fetching (LSE API)
requests>=2.31

# Statistical rigor (tests/statistical_rigor.py only — optional for runtime)
# scipy already declared above

# Notes
# - The runners themselves import: numpy, pandas, hmmlearn, sklearn, matplotlib
# - LSE loader additionally imports requests
# - scipy is required only for the statistical_rigor diagnostic script
